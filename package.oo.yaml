version: 0.0.1
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
name: paper-banana
displayName: "%display-name%"
icon: ":twemoji:banana:"
description: "%description%"
ui:
  blocks:
    - generate
    - retrieve
    - plan
    - visualize
    - edit
