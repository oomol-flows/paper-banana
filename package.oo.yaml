version: 0.1.2
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
name: paper-banana
displayName: "%display-name%"
icon: ":twemoji:banana:"
description: "%description%"
ui:
  blocks:
    - generate
    - retrieve
    - plan
    - visualize
    - edit
repository: https://github.com/oomol-flows/paper-banana
releaseNotes: "fix: change return type to file"
dependencies:
  downloader: 0.1.5
